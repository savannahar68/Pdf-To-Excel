To run the script, install all the dependencies and type the following commands <br/>
Excel folder is needed so that all the files would be saved into it.

  - mkdir excel 
  - python3 seatDistribution_toExcel.py
